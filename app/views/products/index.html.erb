<h1> Productos </h1>

<%  @products.each do |product| %>
    <%= link_to product_path(product.id), class: 'product' do %>
        <h2><%= product.title%> </h2>
    <% end %>
    <h2>$<%= product.price%> </h2>
<% end %>